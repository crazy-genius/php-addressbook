<form method="post" action="{{ path('app_address_in_groups_delete', {'id': address_in_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ address_in_group.id) }}">
    <button class="btn">Delete</button>
</form>
