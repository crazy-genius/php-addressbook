<form method="post" action="{{ path('app_addressbook_delete', {'id': addressbook.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ addressbook.id) }}">
    <button class="btn">Delete</button>
</form>
